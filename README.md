# knn_model
KNN is a simple, instance-based learning algorithm used for SMS spam detection. It classifies messages by measuring similarity (distance) to labeled messages. After text vectorization (e.g., TF-IDF), KNN predicts labels based on the majority class among k nearest neighbors. It works best with balanced, scaled data.
